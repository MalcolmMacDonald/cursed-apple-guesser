Initial task:
Update all the Claude md files to reflect the current state of the project.


Next features:

Left + right arrow keys to step through screenshots

Resize screenshot image panel

Click on a defined tag to automatically apply it to all selected screenshots

Ability to delete images

Include/exclude toggle for filter

Hover over an image in the "x image selected" panel to highlight it on the map
Select an image in the "x Image selected" panel to view it on its own





Future features:

Histogram of missing screenshotted areas/directions (each screenshot has its own frustum that adds to the histogram)

Store all of this data in a database instead of a JSON file, so that we can have multiple users and a web interface to view the data

